# Programming-Year-2

Console.WriteLine("Введіть розміність трикутніка:");
            string na = Console.ReadLine();
            int n = int.Parse(na);
            int[][] triangl = new int[n][];
            for (int i = 0; i < n; i++)
            {
                triangl [i] = new int [] {};
            }
